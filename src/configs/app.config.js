const appConfig = {
    apiPrefix: '/api',
    authenticatedEntryPath: '/products',
    unAuthenticatedEntryPath: '/sign-in',
    tourPath: '/',
    locale: 'en',
    enableMock: true,
}

export default appConfig
